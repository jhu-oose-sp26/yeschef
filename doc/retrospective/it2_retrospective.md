For iteration 2, we completed the following objectives:
- extended the supported classes and operations in the backend - specifically, implemented the User, HasLiked, and HasSaved classes
- built a simple frontend to validate API functionality
- populated database with recipes pulled from external API
- modified database to use DTOs instead of persistence models

What we did well for iteration 2:
- Git branches and pull requests - we all developed in separate branches to minimize conflicts and allow for parallel development
- Collaborated to develop a comprehensive database design
- Finished dependent tasks on schedule so as to not delay group progress 

Challenges we faced in iteration 2 and how we will improve:
- The main challenge we faced was that many of our tasks were dependent on another group member finishing their task. This meant that for many tasks we could not work in parallel, and our progress was dependent on indiviual members' bandwidth and schedule. While we all finished our tasks in a timely fashion, allowing the group to progress at our planned pace, to minimize this issues in future iterations we will segment our tasks to be less dependent so that we can all work in parallel. 